SELECT
    chunk_id
FROM
    (
    SELECT
        chunk_id,
        ( embedding.search_vector <=> '{{query_embedding}}') AS similarity
        FROM embedding
        WHERE collection_id = '{{collection_id}}'
    )
WHERE similarity <= {{vector_score_threshold}}
ORDER BY similarity DESC
LIMIT {{top_n}};